---
title: 软件开发工具实践 - 实验报告合集
date: 2026-07-08
categories: 实验报告
tags:
  - 实验报告
  - 合集
---

这里汇总了本课程的全部实验报告，点击下方页签切换查看。

{% tabs 实验报告合集, 1 %}

<!-- tab 实验一：Web开发环境搭建 -->

writer：许珂

## 1、实验目的

1. 掌握 Node.js、Git、Hexo 的安装与配置方法
2. 理解静态站点生成器（SSG）的工作原理
3. 能够使用 Hexo 在本地构建并预览静态博客站点
4. 熟悉 Web 开发环境的基础工具链

## 2、实验环境

| 项目 | 详情 |
|------|------|
| 操作系统 | Windows 11 |
| Node.js | v24.18.0（LTS 版本） |
| Git | v2.55.0.2 |
| Hexo CLI | v4.3.2 |
| 代码编辑器 | Visual Studio Code |
| 浏览器 | Microsoft Edge |

## 3、实验内容与步骤

### 3.1 安装 Node.js 与 npm

1. 访问 Node.js 官网（或国内镜像站 `https://nodejs.cn/download/`），下载 LTS 版本的 Windows 安装包（`node-v24.18.0-x64.msi`）。
2. 双击安装包，在安装过程中**务必勾选“Add to PATH”**，其余保持默认，一路点击“Next”完成安装。
3. 打开命令提示符（CMD），输入以下命令验证安装是否成功：

```bash
node -v
npm -v
```

4. 配置 npm 国内镜像源，加速后续依赖包的下载：

```bash
npm config set registry https://registry.npmmirror.com
```

{% asset_img 01-node-version.png Node.js版本验证 %}

### 3.2 安装 Git 版本管理工具

1. 访问 Git 官网 `https://git-scm.com/download/win`，下载 Windows 版本安装包。
2. 双击安装包，关键配置项如下：
   - **Select Components**：保持默认，直接点击 Next
   - **Choosing the default editor**：选择 **“Use Visual Studio Code as Git's default editor”**
   - **Adjusting your PATH environment**：选择 **“Git from the command line and also from 3rd-party software”**（中间选项）
   - **Configuring the line ending conversions**：保持默认 **“Checkout Windows-style, commit Unix-style line endings”**
   - **Choosing HTTPS transport backend**：选择 **“Use the OpenSSL library”**
   - **Configuring the terminal emulator**：保持默认 **“Use MinTTY”**
   - **Choose the default behavior of `git pull`**：选择 **“Merge”**
   - **Choosing a credential helper**：保持默认
3. 其余保持默认，一路 Next 完成安装。
4. 验证安装并配置用户信息：

```bash
git --version
git config --global user.name "xk"
git config --global user.email "2660456474@qq.com"
```

{% asset_img 02-git-version.png Git版本验证 %}

### 3.3 安装 Hexo CLI 并初始化博客项目

1. 全局安装 Hexo 命令行工具：

```bash
npm install -g hexo-cli
hexo -v
```

2. 在 D 盘创建项目目录并初始化博客：

```bash
D:
mkdir Blog
cd Blog
hexo init my-blog
```

> **说明**：如果 GitHub 连接超时，Hexo 会自动切换为内置缓存复制，不影响最终结果，耐心等待即可。

3. 进入项目目录并安装项目依赖：

```bash
cd my-blog
npm install
```

{% asset_img 03-hexo-version.png Hexo版本信息 %}

### 3.4 本地预览博客

1. 在项目目录下启动本地预览服务：

```bash
hexo server
```

看到如下提示表示服务启动成功：

```
INFO Hexo is running at http://localhost:4000 . Press Ctrl+C to stop.
```

2. 打开浏览器，访问 `http://localhost:4000`，即可看到 Hexo 默认的博客首页。

{% asset_img 04-localhost-preview.png 本地预览博客首页 %}

### 3.5 创建并发布一篇博客文章

1. 使用以下命令创建一篇新文章：

```bash
hexo new "我的第一篇文章"
```

2. 用 VS Code 打开 `source/_posts/我的第一篇文章.md`，修改 Front-matter 信息和正文内容后保存。

3. 重新生成静态页面并再次预览：

```bash
hexo generate
hexo server
```

{% asset_img 05-vscode-markdown.png VS Code编辑Markdown %}

### 3.6 将源码推送到 GitHub（小组协作准备）

1. 在 GitHub 创建私有仓库，命名为 `GroupWork26.7.6`

2. 初始化 Git 并关联远程仓库：

```bash
git init
git remote add origin https://github.com/你的用户名/GroupWork26.7.6.git
```

3. 创建 `.gitignore` 文件，忽略不需要上传的文件夹：

```bash
echo "node_modules/" >> .gitignore
echo "public/" >> .gitignore
echo ".deploy_git/" >> .gitignore
```

4. 提交并推送到 GitHub：

```bash
git add .
git commit -m "初始化Hexo博客项目"
git branch -M main
git push -u origin main
```

{% asset_img 06-github-repo.png GitHub仓库文件列表 %}

## 4、实验结果

通过本次实验，成功完成了以下目标：

| 检查项 | 状态 |
|--------|------|
| Node.js 安装成功（`node -v` 正常输出） | ✅ |
| npm 安装成功（`npm -v` 正常输出） | ✅ |
| Git 安装成功（`git --version` 正常输出） | ✅ |
| Hexo CLI 安装成功（`hexo -v` 正常输出） | ✅ |
| 本地预览服务正常（`hexo server` 可运行） | ✅ |
| 浏览器可访问 `http://localhost:4000` | ✅ |
| 新文章创建并显示在博客首页 | ✅ |
| 源码已推送到 GitHub 远程仓库 | ✅ |

## 5、遇到的问题与解决方案

| 问题现象 | 可能原因 | 解决方案 |
|----------|----------|----------|
| `nodejs.org` 官网打不开 | 海外服务器访问不稳定 | 改用国内镜像站 `https://nodejs.cn/download/` |
| `hexo init` 克隆超时报错 | GitHub 网络连接被重置 | Hexo 自动切换为内置缓存复制，等待即可 |
| Git 安装后 `git` 命令不被识别 | 安装时未勾选“Add to PATH” | 重新运行安装程序，确保勾选“Add to PATH” |
| `git push` 提示认证失败 | 未配置 Personal Access Token | 在 GitHub 生成 Token，替代密码使用 |
| VS Code 无法识别 `git` 命令 | VS Code 终端未加载 PATH | 重启 VS Code 或重新加载窗口 |

## 6、实验总结

本次实验是《软件开发工具实践》课程的起点，完成了 Web 开发环境的基础搭建。通过安装 Node.js、Git 和 Hexo，成功在本地运行了一个静态博客站点，并将源码推送到 GitHub 进行版本管理。

关键收获：
1. **理解了工具链的依赖关系**：Hexo 依赖 Node.js 运行，npm 负责包管理，Git 负责版本控制
2. **掌握了静态站点生成器的工作流程**：Markdown 编写 → `hexo generate` → 生成静态 HTML → `hexo server` 本地预览
3. **熟悉了 Git 的基本协作流程**：`add` → `commit` → `push` → `pull`

后续实验将在本实验搭建的环境基础上，进一步完成 Linux 虚拟机部署、Nginx 配置、远程管理等内容。

<!-- endtab -->

<!-- tab 实验二：代码编辑与管理 -->

writer：陈诺
## 1. 实验目的
安装VS Code代码编辑器，熟悉软件界面布局；安装Markdown系列扩展插件；掌握编辑器自定义配置；学习Markdown基础语法，完成文档编写与实时预览。

## 2. 实验环境
- 操作系统：Windows 11
- 软件：Visual Studio Code
- 插件：Markdown All in One、Markdown Preview Enhanced、Paste Image

## 3. 实验过程
1. 官网下载VS Code安装包，安装时勾选添加PATH、右键文件打开菜单等附加功能；
2. 启动软件，熟悉活动栏、侧边栏、编辑区、底部面板、状态栏布局；
![侧边栏](/images/2.1.png)
1. 快捷键`Ctrl+Shift+X`打开扩展面板，安装Markdown配套插件；
![安装Markdown配套插件](/images/2.2.png)
1. 设置自动保存、自动单词折行，配置粘贴图片相对路径；
![设置](/images/2.3.png)
1. 新建`.md`文件，编写标题、列表、代码块、引用等Markdown语法，打开预览窗口查看渲染效果。
![渲染效果](/images/2.4.png)

## 4. 实验结果
VS Code正常启动，所有插件成功启用；Markdown文档渲染正常，目录、代码块、图片均可正常展示。

## 5. 知识总结
1. 代码编辑器VS IDE：VS Code轻量、启动快，依靠插件拓展功能；IDE集成编译、调试全套工具；
2. Markdown：轻量级标记语言，语法简洁，可转换HTML、PDF，广泛用于文档、博客；
3. 插件机制：VS Code核心扩展方式，不同插件分别实现目录生成、流程图绘制、文件导出等功能；
4. 相对路径可规避预览图片失效问题，是Markdown写作推荐路径格式。

## 6. 出现问题
1. 终端无法识别`code`命令
    - 原因：安装未勾选添加到系统PATH
    - 解决：重装VS Code并勾选PATH选项
2. Markdown预览窗口空白、加载异常
    - 原因：插件未启用、软件后台进程冲突
    - 解决：启用对应插件，完全重启VS Code
3. Markdown内图片无法显示
    - 原因：使用绝对路径存放图片
    - 解决：修改为相对路径存放图片

## 7. 心得体会
VS Code强大的插件生态使其适配各类开发场景，轻量化特性对比大型IDE启动速度更快。Markdown语法简单易上手，是技术文档、笔记、博客的标准写作格式。编辑器预览类故障大多由插件、残留进程导致，遇到异常优先重启软件、检查插件状态，可快速解决大部分渲染问题。


<!-- endtab -->

<!-- tab 实验三：虚拟机安装与配置 -->

writer：胡晨妍

## 1、实验目的

本次实验旨在安装Visual Studio Code（VS Code）文本代码编辑器，安装和配置Markdown相关插件，掌握代码编辑器插件的基本概念，学习Markdown基础语法，并使用VS Code完成Markdown文档的编辑和渲染任务。

## 2、实验环境

-操作系统：Windows 11 专业版 22H2
-软件版本：Visual Studio Code 1.85.0
-VS Code插件：Markdown All in One v3.6.0、Markdown Preview Enhanced v0.8.0、Paste Image v1.0.4、markdownlint v0.55.0
-网络：互联网连接（用于下载VS Code及插件）

## 3、实验过程

### 步骤一：安装Visual Studio Code

访问VS Code官网下载安装包，双击启动安装向导。在"选择附加任务"页面，勾选以下选项：
-创建桌面快捷方式
-将"通过Code打开"操作添加到Windows资源管理器文件/目录上下文菜单
-将Code注册为受支持的文件类型的编辑器
-添加到PATH（必须勾选）

点击"安装"完成安装。

![图1：VS Code安装时界面](/images/3.步骤一.png)

### 步骤二：安装Markdown相关插件

点击左侧活动栏中的扩展图标（或按 `Ctrl+Shift+X`），依次搜索并安装以下插件：

1. **Markdown All in One** — 提供目录生成、快捷键支持、自动完成、列表自动延续等功能
2. **Markdown Preview Enhanced** — 提供数学公式（LaTeX）、流程图（Mermaid）、导出为PDF/HTML等功能
3. **Paste Image** — 支持直接粘贴剪贴板中的图片到Markdown文件
4. **markdownlint** — 检查Markdown语法错误和格式规范

![图2：已安装的Markdown相关插件列表](/images/3.步骤二.png)

### 步骤三：配置编辑器优化写作体验

按 `Ctrl+,` 打开设置，进行以下配置：
-`files.autosave` → 设置为 `afterDelay`（开启自动保存）
-`editor.wordwrap` → 设置为 `on`（开启单词折行）
-Paste Image插件：设置 `Path Type` 为 `relative`

![图3：VS Code自动保存和单词折行设置](/images/3.步骤三.png)

### 步骤四：创建并编辑Markdown文件

新建文件（`Ctrl+N`），保存为 `test.md`，输入以下内容：

```markdown
# 我的第一个Markdown文档

## 什么是Markdown？

Markdown是一种**轻量级**的标记语言。

## Markdown的核心特点

-**简洁性**：仅需少量符号即可完成排版
-**可读性**：纯文本仍然清晰易读
-**兼容性**：支持GitHub、博客平台等多种场景

## 一个代码示例

```javascript
function hello() {
    console.log("Hello, Markdown!");
}
```

使用 `Ctrl+Shift+V` 打开预览窗口，实时查看渲染效果。

![图4：VS Code中Markdown编辑与实时预览效果](/images/3.步骤四.png)

## 4、实验结果

| 测试项 | 操作 | 结果 |
| VS Code安装 | 启动VS Code | ✓ 成功启动 |
| Markdown All in One安装 | 扩展面板搜索安装 | ✓ 安装成功，自动完成和目录生成功能正常 |
| Markdown Preview Enhanced安装 | 扩展面板搜索安装 | ✓ 安装成功，预览渲染正常 |
| 快捷键 `Ctrl+Shift+V` | 打开预览窗口 | ✓ 预览窗口正常显示 |
| 粘贴图片 | `Ctrl+Alt+V` | ✓ 图片成功粘贴并保存到相对路径 |

## 5、知识总结

**代码编辑器 vs 集成开发环境（IDE）：**

| 类型 | 特点 | 典型代表 |
|------|------|----------|
| 代码编辑器 | 轻量级、启动快、以编辑为核心功能、通过插件扩展能力 | VS Code、Sublime Text、Notepad++ |
| 集成开发环境（IDE） | 功能全面、集成了编译/调试/构建/版本控制等全套工具链 | IntelliJ IDEA、PyCharm、Eclipse |

**Markdown核心语法：**

| 语法元素 | Markdown写法 | 渲染效果 |
|----------|--------------|----------|
| 一级标题 | `# 标题` | <h1>标题</h1> |
| 二级标题 | `## 标题` | <h2>标题</h2> |
| 加粗 | `**文本**` | **文本** |
| 列表 | `- 项目` | • 项目 |
| 代码块 | \```language ... \``` | 带语法高亮的代码块 |
| 链接 | `[文本](URL)` | 可点击链接 |
| 图片 | `![alt](路径)` | 嵌入图片 |

## 6、出现问题

| 问题现象 | 可能原因 | 解决方法 |
|----------|----------|----------|
| 安装VS Code后无法在命令行使用code命令 | 安装时未勾选"添加到PATH" | 重新运行安装程序勾选"添加到PATH"；或手动将VS Code安装目录添加到系统环境变量 |
| 扩展面板搜索不到插件 | 网络问题或搜索关键词不准确 | 检查网络连接；使用准确的插件全名搜索 |
| `[TOC]`不生成目录 | 未安装Markdown All in One插件 | 安装并启用Markdown All in One插件 |
| 预览中图片无法显示 | 图片路径错误 | 检查图片路径是否正确，建议使用相对路径 |

## 7、心得体会

通过本次实验，我深刻体会到"工欲善其事，必先利其器"的道理。VS Code作为一款轻量级但功能强大的代码编辑器，其插件生态极为丰富。Markdown All in One和Markdown Preview Enhanced等插件将编辑器从单纯的文本编辑工具扩展为高效的写作平台。Markdown的简洁语法让我能够专注于内容本身而非排版细节，这对于技术写作来说尤为重要。同时，我也认识到插件管理是现代软件开发工具的核心能力之一——通过插件组合，一个基础工具可以适应各种不同的应用场景。
<!-- endtab -->

<!-- tab 实验四：Web站点构建 -->

writer：胡晨妍

## 1、实验目的

本次实验旨在使用静态站点构建工具（Hexo）构建一个静态Web博客站点，了解和掌握Web应用开发的基本要素与流程（环境准备→项目初始化→内容编写→主题配置→本地预览→部署上线），理解静态站点生成器（SSG）的工作原理及其与动态站点的区别。

## 2、实验环境

- 操作系统：Windows 11 专业版 22H2
- 软件版本：Node.js v20.10.0，npm v10.2.3，Git 2.43.0，Hexo CLI 4.3.0
- 部署平台：GitHub Pages
- 博客主题：NexT v8.19.0
- 文本编辑器：Visual Studio Code 1.85.0
- 网络：互联网连接（需访问GitHub）

## 3、实验过程

### 步骤一：验证环境与安装Hexo CLI

确认Node.js、npm和Git已正确安装：

```bash
node -v    # v20.10.0
npm -v     # 10.2.3
git --version  # git version 2.43.0
```

全局安装Hexo CLI：

```bash
npm install -g hexo-cli
hexo -v    # 验证安装
```

![图1：环境依赖版本验证](/images/4.步骤一.png)

### 步骤二：初始化博客项目

```bash
cd D:\Blog
hexo init my-blog
cd my-blog
npm install
```

初始化完成后，项目目录结构如下：

```
my-blog/
  ├── _config.yml          # 站点配置文件
  ├── package.json         # 项目依赖清单
  ├── scaffolds/           # 文章模板
  ├── source/              # 内容源文件目录
  │   └── _posts/          # 文章存放目录
  └── themes/              # 主题目录
```

![图2：Hexo博客项目目录结构](/images/4.步骤二.PNG)

### 步骤三：本地预览与创建文章

```bash
hexo server                # 启动本地预览
```

访问 http://localhost:4000 查看默认博客。创建文章并生成：

```bash
hexo new "Web站点构建实验"
hexo generate              # 生成静态页面
```

![图3：创建的新文章在博客首页显示](/images/4.步骤三.png)

### 步骤四：更换NexT主题

```bash
git clone https://github.com/theme-next/hexo-theme-next themes/next
```

修改 `_config.yml` 中的 `theme` 字段为 `next`，重新生成并预览。

![图4：应用NexT主题后的博客首页](/images/4.步骤五.png)

### 步骤五：部署到GitHub Pages

创建GitHub仓库（命名为 `用户名.github.io`），安装部署插件并配置：

```bash
npm install hexo-deployer-git --save
```

在 `_config.yml` 末尾添加：

```yaml
deploy:
  type: git
  repo: https://github.com/你的用户名/你的用户名.github.io.git
  branch: main
```

执行部署：

```bash
hexo clean && hexo generate && hexo deploy
```

访问 `https://你的用户名.github.io` 查看上线博客。

![图5：部署到GitHub Pages后的线上博客](/images/4.步骤四.PNG)

## 4、实验结果

| 测试项 | 操作 | 结果 |
|--------|------|------|
| 项目初始化 | hexo init | ✓ 目录结构完整生成 |
| 本地预览 | hexo server | ✓ http://localhost:4000 可访问 |
| 文章创建 | hexo new | ✓ Markdown文件生成，页面更新正常 |
| 主题更换 | git clone + 修改配置 | ✓ NexT主题成功应用 |
| GitHub Pages部署 | hexo deploy | ✓ https://用户名.github.io 可访问 |

## 5、知识总结

**静态站点生成器（SSG）工作原理：**

```
Markdown源文件 + 主题模板 + 配置文件
        ↓
   [Hexo构建]
        ↓
  纯HTML/CSS/JS静态文件
        ↓
   [部署到Web服务器]
        ↓
      用户访问
```

**静态站点 vs 动态站点：**

| 对比维度 | 静态站点（SSG） | 动态站点（如WordPress） |
|----------|----------------|------------------------|
| 页面生成时机 | 构建时预先生成所有页面 | 用户访问时由服务器实时生成 |
| 数据库 | 不需要 | 通常需要 |
| 访问速度 | 极快（纯静态文件） | 较慢（需执行后端代码+查数据库） |
| 安全性 | 高（无后端攻击面） | 需额外防护 |
| 部署方式 | 上传静态文件即可 | 需配置服务器环境和数据库 |
| 适用场景 | 博客、文档站、个人网站 | 电商、社交、CMS |

**Web应用开发流程（本实验覆盖）：**

环境准备 → 项目初始化 → 内容编写 → 主题配置 → 本地预览 → 部署上线

## 6、出现问题

| 问题现象 | 可能原因 | 解决方法 |
|----------|----------|----------|
| 'hexo'不是内部或外部命令 | Hexo CLI未正确安装 | 执行 `npm install -g hexo-cli` 重新安装 |
| hexo init速度极慢或失败 | 网络问题，无法访问GitHub | 配置代理，或使用Gitee镜像源 |
| hexo server启动后浏览器无法访问 | 端口4000被占用 | 更换端口：`hexo server -p 4001` |
| 部署后GitHub Pages显示404 | 仓库名称不是 `用户名.github.io` | 检查仓库命名是否正确 |
| 部署后页面样式丢失 | 站点URL配置错误 | 检查 `_config.yml` 中 `url` 字段设置为 `https://用户名.github.io` |

## 7、心得体会
   通过本次实验，我完整体验了从零开始构建并部署一个Web站点的全过程。从环境准备到本地开发，再到主题定制和最终部署，每一步都让我对Web应用开发的完整流程有了清晰的认识。
    静态站点生成器（SSG）的理念让我印象深刻——"提前做好"而非"现场制作"。这种模式在博客、文档站等场景中具有显著优势：速度快、安全性高、成本低。GitHub Pages提供的免费托管服务极大地降低了个人建站的门槛。
    在部署过程中，我特别感受到了开源生态的力量。Hexo、NexT主题、GitHub Pages都是开源项目，它们组合在一起形成了一个完整、高效且免费的建站解决方案。作为未来的软件开发者，积极参与开源社区、善用开源工具、回馈开源生态，是我们应有的责任与担当。

<!-- endtab -->

<!-- tab 实验五：Linux 环境配置与基本操作-->

writer：王玉晶
## 1、实验目的

本次实验旨在掌握 Linux 系统下用户账户的创建、查看、修改和删除操作；理解用户组的概念，掌握组的管理方法；理解 Linux 文件权限模型（读、写、执行），掌握使用 `chmod` 和 `chown` 命令修改文件权限和所有者。

## 2、实验环境

| 项目 | 内容 |
|------|------|
| 操作系统 | Windows 11 专业版 |
| 虚拟机软件 | Oracle VirtualBox 7.2.12 |
| 虚拟机系统 | Ubuntu Server 22.04.5 LTS |
| 实验节点 | node1（192.168.56.102） |
| 连接方式 | SSH 远程登录（端口 2223） |
| 初始用户 | vboxuser（具有 sudo 权限） |

## 3、实验过程

### 步骤一：用户管理——创建新用户

通过 SSH 连接到 node1，使用 `adduser` 命令创建名为 `testuser` 的新用户：

```bash
sudo adduser testuser
```

执行过程中，系统提示设置密码（设置为 `123456`），其他信息（全名、房间号等）直接按回车跳过，最后输入 `Y` 确认。

![创建新用户](/images/5.1.jpg)

### 步骤二：用户管理——查看用户信息

查看 `/etc/passwd` 文件，确认用户已成功创建：

```bash
cat /etc/passwd | grep testuser
```

输出结果：

```
testuser:x:1001:1001:..:/home/testuser:/bin/bash
```

`/etc/passwd` 各字段含义：

| 字段 | 说明 |
|------|------|
| `testuser` | 用户名 |
| `x` | 密码占位符（实际密码存储在 `/etc/shadow`） |
| `1001` | UID（用户 ID） |
| `1001` | GID（主组 ID） |
| `/home/testuser` | 用户主目录 |
| `/bin/bash` | 登录 Shell |

### 步骤三：用户管理——授予 sudo 权限

将 `testuser` 添加到 `sudo` 组，使其拥有执行管理员命令的权限：

```bash
sudo usermod -aG sudo testuser
```

查看用户所属组：

```bash
groups testuser
```

输出：

```
testuser : testuser sudo
```

### 步骤四：用户管理——切换用户

切换到 `testuser`，验证用户是否可正常登录：

```bash
su - testuser
```

输入密码后，提示符变为 `testuser@node1:~$`，说明切换成功。

![切换用户验证](/images/5.2.jpg)

返回 `vboxuser`：

```bash
exit
```

### 步骤五：组管理——创建组

创建名为 `devgroup` 的新组，并将 `vboxuser` 加入该组：

```bash
sudo groupadd devgroup
sudo usermod -aG devgroup vboxuser
groups vboxuser
```

输出：

```
vboxuser : vboxuser adm cdrom sudo dip plugdev lxd devgroup
```

![组管理操作](/images/5.3.jpg)

### 步骤六：文件权限管理——创建并查看文件

创建测试文件并查看初始权限：

```bash
touch test.txt
ls -l test.txt
```

输出：

```
-rw-rw-r-- 1 vboxuser vboxuser 0 Jul 11 11:51 test.txt
```

权限解读：`-rw-rw-r--` 表示这是一个普通文件，所有者可读可写，组内用户可读可写，其他用户只读。

### 步骤七：文件权限管理——数字法修改权限

使用数字法将文件权限修改为 `755`：

```bash
chmod 755 test.txt
ls -l test.txt
```

输出：

```
-rwxr-xr-x 1 vboxuser vboxuser 0 Jul 11 11:51 test.txt
```

**权限数字对照表：**

| 数字 | 权限 | 含义 |
|------|------|------|
| 7 | rwx | 读 + 写 + 执行 |
| 6 | rw- | 读 + 写 |
| 5 | r-x | 读 + 执行 |
| 4 | r-- | 只读 |
| 0 | --- | 无权限 |

`755` 表示：所有者 `7`（rwx 可读可写可执行），组 `5`（r-x 可读可执行），其他人 `5`（r-x 可读可执行）。

### 步骤八：文件权限管理——修改所有者

将文件所有者修改为 `root`：

```bash
sudo chown root test.txt
ls -l test.txt
```

输出：

```
-rwxr-xr-x 1 root vboxuser 0 Jul 11 11:51 test.txt
```

文件所有者从 `vboxuser` 变为 `root`。

![修改文件所有者](/images/5.4.jpg)

## 4、实验结果

### 4.1 用户管理

| 操作 | 命令 | 结果 |
|------|------|------|
| 创建用户 | `sudo adduser testuser` | ✅ 成功创建，UID=1001 |
| 查看用户信息 | `cat /etc/passwd \| grep testuser` | ✅ 显示用户信息 |
| 授予 sudo 权限 | `sudo usermod -aG sudo testuser` | ✅ testuser 在 sudo 组中 |
| 切换用户 | `su - testuser` | ✅ 切换成功 |

![用户管理结果](/images/5.5.jpg)

### 4.2 组管理

| 操作 | 命令 | 结果 |
|------|------|------|
| 创建组 | `sudo groupadd devgroup` | ✅ 创建成功 |
| 添加用户到组 | `sudo usermod -aG devgroup vboxuser` | ✅ vboxuser 在 devgroup 中 |
| 查看用户组 | `groups vboxuser` | ✅ 显示所有组 |

![组管理结果](/images/5.6.jpg)

### 4.3 文件权限

| 操作 | 命令 | 修改前 | 修改后 |
|------|------|--------|--------|
| 查看权限 | `ls -l test.txt` | `-rw-rw-r--` | `-rwxr-xr-x` |
| 修改权限 | `chmod 755 test.txt` | 644 | 755 |
| 修改所有者 | `sudo chown root test.txt` | vboxuser | root |

![文件权限结果](/images/5.7.jpg)

## 5、知识总结

### 5.1 用户相关命令

| 命令 | 作用 |
|------|------|
| `sudo adduser 用户名` | 创建新用户 |
| `cat /etc/passwd` | 查看所有用户信息 |
| `sudo usermod -aG 组名 用户名` | 将用户添加到组 |
| `groups 用户名` | 查看用户所属组 |
| `su - 用户名` | 切换用户 |
| `sudo deluser 用户名` | 删除用户 |

![用户命令汇总](/images/5.8.jpg)

### 5.2 组相关命令

| 命令 | 作用 |
|------|------|
| `sudo groupadd 组名` | 创建新组 |
| `cat /etc/group` | 查看所有组信息 |
| `sudo groupdel 组名` | 删除组 |

![组命令汇总](/images/5.9.jpg)

### 5.3 权限管理命令

| 命令 | 作用 |
|------|------|
| `chmod 755 文件名` | 数字法修改权限 |
| `chmod u+x 文件名` | 符号法：所有者增加执行权限 |
| `chmod g-w 文件名` | 符号法：组去掉写权限 |
| `sudo chown 用户 文件名` | 修改文件所有者 |
| `sudo chgrp 组名 文件名` | 修改文件所属组 |
| `ls -l 文件名` | 查看文件权限 |

![权限命令汇总](/images/5.10.jpg)

## 6、出现问题

| 问题现象 | 可能原因 | 解决方案 |
|----------|----------|----------|
| 在 Windows CMD 中执行了 `sudo adduser testuser` | Windows 不识别 Linux 命令 | 先通过 SSH 连接到 Linux 虚拟机，再执行命令 |

## 7、心得体会

通过本次实验，我深入学习了 Linux 用户管理、组管理和文件权限管理，掌握了 `adduser`、`usermod`、`chmod`、`chown` 等核心命令的使用方法。通过实际操作创建用户、配置权限，我对 Linux 多用户系统的权限模型有了更直观的理解。

在实验过程中，我遇到的最常见问题是在 Windows 下误用了 Linux 命令，这提醒我要注意区分不同系统的命令行环境。通过 SSH 连接到虚拟机后再执行命令，问题就迎刃而解了。

本次实验使用的 VirtualBox、Ubuntu、OpenSSH 都是开源软件，免费且文档完善，这让我感受到了开源生态的便利。国内也有类似的开源项目在发展，但生态建设上还有提升空间。作为学习者，我会继续深入学习 Linux 系统管理，为后续的服务器运维打下坚实基础。

<!-- endtab -->

<!-- tab 实验六：Linux 账户与权限管理 -->

writer：许润晗
## 1、实验目的

1. 掌握登录后查看用户、系统内核、系统版本、主机名、工作目录等基础信息命令。

2. 熟练使用命令完成目录切换、文件/文件夹创建、复制、移动、删除等文件系统操作。

3. 学会查看内存、磁盘、进程、目录占用大小等系统资源状态。

4. 掌握文件内容查看、输出重定向、管道、通配符的基础使用。

5. 学会使用apt工具完成软件源更新、软件安装与卸载操作。

6. 读懂/etc/passwd、/etc/shadow、/etc/group三大用户配置文件，理解用户字段含义。

7. 熟练完成用户创建、设置密码、赋予管理员权限、切换用户、删除用户全套操作。

8. 熟记Linux权限数字对照表，掌握数字法、符号法两种chmod权限修改方式。

9. 使用chown修改文件所有者与所属组，chgrp单独修改文件所属用户组。

10. 区分普通用户与root管理员权限，理解sudo临时提权机制。

11. 掌握环境变量查看、临时环境变量创建与删除。

12. 区分用户级永久环境变量与系统级全局环境变量，掌握三种系统配置文件写法。

13. 理解各配置文件加载时机、生效范围，学会source命令即时刷新配置。

14. 掌握自定义命令别名alias配置。

15. 熟悉环境配置常见报错排查方法。

## 2、实验环境

• 宿主机操作系统：Windows 10

• 虚拟化软件：VirtualBox

• Linux系统：Ubuntu Server 22.04 LTS

• 实验虚拟机：template

## 3、实验操作步骤

### 第一部分：实验四 Linux基本操作

步骤一：登录与查看系统信息

登录Ubuntu终端，依次执行如下命令查看系统基础信息：
whoami               # 查看当前登录用户
uname -a             # 查看内核与硬件架构完整信息
lsb_release -a       # 查看Ubuntu系统版本信息
hostname             # 查看本机主机名
pwd                  # 查看当前所在工作目录
![](/images/6.1.jpg)
步骤二：文件与目录的基本操作

完成文件夹、文件创建、复制、移动、删除等操作：
ls -lh                    # 列出目录内容，展示详细权限、人性化文件大小
cd ~                      # 切换到用户家主目录
mkdir test_dir            # 创建文件夹test_dir
touch test.txt            # 创建空白文本文件test.txt
cp test.txt test_copy.txt # 复制test.txt为test_copy.txt
mv test.txt /tmp/         # 将test.txt移动到/tmp临时目录
rm -f test.txt            # 强制删除文件test.txt
![](/images/6.2.jpg)
步骤三：查看系统资源状态

监控内存、磁盘、进程、文件夹占用资源：
free -h           # 人性化单位查看内存使用情况
df -h             # 查看磁盘分区整体占用
top               # 实时查看系统进程、CPU与内存占用，按q退出界面
du -sh *          # 查看当前目录下各个子目录总占用大小
![](/images/6.3.jpg)
步骤四：文件内容查看、管道与重定向

1. 文件读取查看命令
cat /etc/hosts                  # 一次性完整输出文件内容
less /etc/netplan/*.yaml        # 分页浏览配置文件，上下翻页查看
head /var/log/auth.log          # 查看日志文件前10行
tail /var/log/auth.log          # 查看日志文件末尾10行
tail -f /var/log/auth.log       # 实时追踪日志新增内容
2. 输出重定向
ls -lh > filelist.txt   # 将目录列表覆盖写入filelist.txt
pwd >> filelist.txt     # 将当前路径追加写入文件，不覆盖原有内容
3. 管道过滤操作
ip a | grep inet        # 查看网卡信息，筛选出IP地址行
ls -l /etc | less       # 目录列表分页查看
4. 通配符批量匹配文件
ls *.txt    # 匹配所有后缀为txt的文件
ls test*    # 匹配所有以test开头的文件、文件夹
步骤五：apt软件包管理
sudo apt update         # 更新本地软件源索引缓存
sudo apt install tree   # 安装tree目录树形查看工具
sudo apt remove tree    # 卸载已安装的tree软件
![](/images/6.5.jpg)
![](/images/6.6.jpg)
![](/images/6.7.jpg)
### 第二部分：实验五 Linux账户与权限管理

步骤六：查看用户与组配置文件
cat /etc/passwd                # 用户账户基础信息
sudo cat /etc/shadow           # 用户加密密码文件，仅root可读
cat /etc/group                 # 系统所有用户组信息
id                             # 查看当前用户UID、GID、附加用户组
/etc/passwd 文件字段格式：
用户名:密码占位符:UID:GID:用户描述:主目录:登录Shell
![](/images/6.8.jpg)
![](/images/6.9.jpg)
步骤七：创建和管理用户
sudo adduser testuser                  # 创建新用户testuser
sudo passwd testuser                   # 为testuser设置登录密码
sudo usermod -aG sudo testuser          # 将用户加入sudo组，赋予管理员权限
groups testuser                        # 查看testuser所属全部用户组
su - testuser                          # 切换登录为testuser用户
sudo deluser -r testuser               # 删除用户并同步删除家目录
![](/images/6.10.jpg)
![](/images/6.11.jpg)
![](/images/6.12.jpg)
![](/images/6.13.jpg)
步骤八：文件权限管理

权限数字对照表
数字 权限 含义 
7 rwx 读+写+执行 
6 rw- 读+写 
5 r-x 读+执行 
4 r-- 只读 
0 --- 无任何权限 

权限修改与归属操作命令
chmod 755 test.txt                     # 数字法修改文件权限
chmod u+x test.txt                    # 符号法：给文件所有者增加执行权限
chmod g-w test.txt                    # 符号法：移除同组用户的写权限
sudo chown root:root test.txt         # 修改文件所有者为root，所属组为root
sudo chgrp www-data test.txt          # 仅修改文件所属用户组为www-data
### 第三部分：实验六 Linux环境配置

步骤九：查看环境变量
env                  # 查看系统全部环境变量
echo $PATH           # 查看PATH可执行文件搜索路径
echo $HOME           # 查看当前用户家目录
echo $USER           # 查看当前登录用户名
![](/images/6.14.jpg)
步骤十：临时设置环境变量

仅当前终端会话有效，关闭终端后失效。
**追加自定义目录到PATH**
export PATH=$PATH:/home/dev/mybin
**创建自定义变量**
export MY_VAR="Hello World"
**验证变量**
echo $MY_VAR
**除自定义临时变量**
unset MY_VAR
![](/images/6.15.jpg)
步骤十一：永久设置环境变量（用户级，仅当前用户生效）
配置文件 加载时机 作用范围 
~/.bashrc 每次打开新终端时执行 当前用户 
~/.profile 用户登录时执行一次 当前用户 
~/.bash_profile 用户登录时执行（优先级高于.profile） 当前用户 

编辑用户配置文件：
nano ~/.bashrc
文件末尾添加内容：
**扩展程序搜索路径**
export PATH=$PATH:/home/dev/mybin
**自定义环境变量**
export MY_CUSTOM_VAR="自定义变量"
**自定义命令别名**
alias ll='ls -alF'
保存退出，立即生效：
source ~/.bashrc
![](/images/6.16.jpg)
步骤十二：永久设置环境变量（系统级，所有用户生效）

方法1：/etc/environment
修改后需重启系统才生效，无需export，直接赋值字符串：
sudo nano /etc/environment
写入内容：
PATH="/usr/local/sbin:...:/my/custom/path"
方法2：/etc/profile
所有用户登录时自动加载，支持export，修改后可手动刷新：
sudo nano /etc/profile
文件末尾添加：
export JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64
export PATH=$PATH:$JAVA_HOME/bin
立即全局生效
source /etc/profile
方法3（推荐）：/etc/profile.d/
新建独立配置脚本，便于维护，系统自动读取该目录所有.sh文件：
sudo nano /etc/profile.d/myenv.sh
写入全局变量
export MY_GLOBAL_VAR="全局变量"
刷新生效
source /etc/profile.d/myenv.sh
![](/images/6.17.jpg)
## 4、实验过程遇到的问题与解决方案

1. 问题：使用rm删除文件夹提示“目录不为空”
原因：rm命令默认仅支持删除文件，无法直接删除文件夹。
解决：使用递归强制删除命令 rm -rf 文件夹名。

1. 问题：执行apt install提示权限不足
原因：系统软件安装修改系统文件，普通用户无操作权限。
解决：命令前添加sudo，临时获取管理员权限执行。

## 5、实验成果

1. 熟练使用各类基础命令查看系统、用户、磁盘、进程信息，完成文件与目录全套增删改查操作。

2. 掌握重定向、管道、通配符文本处理逻辑，可使用apt完成软件安装、更新、卸载。

3. 读懂系统三大用户配置文件，完整完成用户创建、授权、切换、删除流程。

4. 掌握数字、符号两种权限修改方式，可修改文件所有者与用户组，理解sudo临时提权机制。

5. 成功查看系统内置环境变量PATH、HOME、USER，掌握env、echo $变量名查看语法。

6. 完成临时环境变量MY_VAR创建、读取、删除，验证临时变量仅当前终端有效。

7. 在~/.bashrc中配置用户级永久变量、扩展PATH路径、自定义ll别名，新开终端配置自动加载。

8. 分别使用/etc/profile、/etc/profile.d/两种方式配置全局环境变量，实现所有登录用户共享变量。

9. 完成Java开发环境全局配置，设置JAVA_HOME并写入系统PATH，任意终端可直接调用java命令。

10. 记录并解决变量失效、sudo命令缺失、权限不足、文件夹删除失败等故障，形成完整Linux运维排错流程。

## 6、实验总结

本次综合实验整合Linux基础操作、账户权限管理、系统环境配置三大模块内容。首先掌握系统信息查询、文件目录操作、资源监控、软件包管理等入门核心命令；其次学习Linux用户、用户组完整生命周期管理，区分普通用户与root权限边界，熟练配置文件读写执行权限；最后区分临时变量、用户级永久变量、系统全局变量三种环境变量配置方式，熟练使用.bashrc、/etc/profile、/etc/profile.d配置自定义变量、程序路径与命令别名。

通过完整实操，能够独立完成Linux基础运维工作，排查权限不足、配置失效、命令找不到等常见报错，为后续虚拟机集群、SSH远程管理、服务器项目部署打下扎实基础。
<!-- endtab -->

<!-- tab 实验七：远程登录管理 -->

writer：陈诺
## 1. 实验目的
安装、配置OpenSSH远程登录服务；配置SSH密钥免密登录；使用SCP、rsync完成跨服务器文件传输与增量同步。

## 2. 实验环境
两台Ubuntu Server虚拟机，Host-Only内网网段`192.168.56.0/24`

## 3. 实验过程
1. 安装`openssh-server`，设置SSH服务开机自启，修改`sshd_config`禁止root远程登录；
2. 使用`ssh-keygen`生成Ed25519公私密钥对，`ssh-copy-id`推送公钥至远程服务器；
![ssh-keygen](/images/7.1.png)
![ssh-copy-id](/images/7.2.png)
3. 测试免密SSH远程登录；
![远程登录](/images/7.3.png)
4. 使用`scp`上传、下载单文件与文件夹；安装rsync工具，实现跨服务器增量文件同步。
![](/images/7.4.png)
![](/images/7.5.png)
![](/images/7.6.png)
![](/images/7.7.png)

## 4. 实验结果
SSH服务正常运行，支持密码、密钥两种登录方式；免密登录配置完成；SCP、rsync均可稳定传输、同步服务器文件。

## 5. 知识总结
1. SSH：加密远程登录协议，默认22端口，保障公网、内网传输数据安全；
2. 两种认证方式对比：
    - 密码认证：操作简单，安全性较低，适合临时测试；
    - 密钥认证：公私钥配对验证，安全性高，生产环境推荐使用；
3. 文件传输工具：
    - SCP：简易一次性文件复制，适合少量文件传输；
    - rsync：增量同步，仅传输修改文件，适合定期备份、目录同步。

## 6. 出现问题
1. SSH连接提示`Connection refused`
    - 原因：远程服务器SSH服务未启动
    - 解决：`sudo systemctl start ssh`启动服务并设置开机自启
2. 配置密钥后登录仍需要输入密码
    - 原因：`~/.ssh/authorized_keys`文件权限过高
    - 解决：执行`chmod 600 ~/.ssh/authorized_keys`修改权限
3. 远程服务器提示`rsync: command not found`
    - 原因：目标服务器未安装rsync工具
    - 解决：远程执行`sudo apt install rsync -y`安装工具

## 7. 心得体会
SSH是远程管理Linux服务器的标准工具，密钥登录相比密码登录安全系数更高，正式服务器应当关闭密码登录功能。rsync增量同步相比SCP传输效率更高，适合项目、日志长期备份。多服务器运维场景下，掌握远程文件同步工具可以极大减少重复文件传输操作，提升运维效率。

<!-- endtab -->

<!-- tab 实验八：Web 组件部署 -->

writer：许珂
## 1、实验目的

1. 综合运用前七个实验所学的全部工具与技能
2. 完整体验 Web 静态站点从开发到部署的全过程
3. 掌握 Linux 服务器（Nginx）的安装与配置方法
4. 理解 CI/CD（持续集成/持续部署）的基本概念
5. 实现基于 GitHub + Hexo + Nginx 的自动化部署流程
6. 培养从零搭建完整 Web 服务的能力

## 2、实验环境

| 项目 | 详情 |
|------|------|
| 开发环境 | Windows 11 + VS Code + Node.js 20.x + Git |
| 静态站点生成器 | Hexo v8.1.2 |
| 主题 | NexT v8.12.1 |
| 版本控制 | GitHub（私有仓库） |
| 服务器操作系统 | Ubuntu Server 22.04.5 LTS（虚拟机） |
| Web 服务器 | Nginx 1.18.0 |
| 部署方式 | 手动 git pull + hexo generate + sudo cp |
| 访问方式 | NAT + 端口转发（宿主机 8080 → 虚拟机 80） |
| 组员访问地址 | `http://宿主机IP:8080` |

## 3、实验内容与步骤

### 3.1 整体架构设计

本实验的整体架构分为四个层次：

```
┌─────────────────────────────────────────────────────────────────┐
│                        开发阶段（主机）                         │
├─────────────────────────────────────────────────────────────────┤
│  组员各自在本地写 Markdown 文章 → git push 到 GitHub           │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        存储阶段（GitHub）                       │
├─────────────────────────────────────────────────────────────────┤
│  GitHub 私有仓库作为云端中央代码库，存储所有源码                │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        构建阶段（虚拟机）                       │
├─────────────────────────────────────────────────────────────────┤
│  git pull → npm install → hexo generate → 生成 public/        │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        部署阶段（虚拟机）                       │
├─────────────────────────────────────────────────────────────────┤
│  sudo cp -r public/* /var/www/html/ → Nginx 提供服务           │
│  用户通过 http://宿主机IP:8080 访问                             │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 第一阶段：源码管理与协作开发

#### 3.2.1 创建 GitHub 仓库

1. 登录 GitHub，点击右上角 **“+”** → **“New repository”**
2. Repository name 填 `GroupWork26.7.6`
3. 选择 **Private**（私有仓库）
4. **不要勾选** “Add a README file”
5. 点击 **“Create repository”**

#### 3.2.2 邀请组员协作

1. 进入仓库页面 → **Settings** → **Collaborators**
2. 点击 **“Add people”**，输入组员的 GitHub 用户名
3. 组员接受邀请后即拥有推送权限

#### 3.2.3 组员分支开发流程

每个组员在自己电脑上：

```bash
# 克隆仓库
git clone https://github.com/k-1000-x/GroupWork26.7.6.git
cd GroupWork26.7.6

# 安装依赖
npm install

# 创建个人分支
git checkout -b chennian

# 写文章
hexo new "文章标题"
# 编辑 source/_posts/ 下的 .md 文件

# 提交
git add .
git commit -m "chennian: 添加文章"
git push -u origin chennian
```

#### 3.2.4 Pull Request 合并

1. 组员在 GitHub 上创建 Pull Request（`chennian` → `main`）
2. 组长审核后点击 **“Merge pull request”**
3. 所有组员拉取最新代码：

```bash
git checkout main
git pull origin main
```


### 3.3 第二阶段：服务器环境准备

#### 3.3.1 安装 Nginx

在虚拟机中执行：

```bash
sudo apt update
sudo apt install nginx -y
```

#### 3.3.2 验证 Nginx 安装

```bash
sudo systemctl status nginx
```

确认显示 `active (running)`。

#### 3.3.3 配置 Nginx 站点

创建站点配置文件：

```bash
sudo tee /etc/nginx/sites-available/blog <<EOF
server {
    listen 80;
    server_name _;
    root /var/www/html;
    index index.html;

    location / {
        try_files \$uri \$uri/ =404;
    }
}
EOF
```

启用站点：

```bash
sudo ln -s /etc/nginx/sites-available/blog /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```


### 3.4 第三阶段：部署

#### 3.4.1 在虚拟机中拉取代码

```bash
cd ~
git clone https://github.com/k-1000-x/GroupWork26.7.6.git
cd GroupWork26.7.6
```

#### 3.4.2 安装项目依赖

```bash
npm install
```

#### 3.4.3 生成静态网页

```bash
hexo generate
```

`public/` 目录即完整的静态站点文件。

#### 3.4.4 部署到 Nginx

```bash
sudo cp -r public/* /var/www/html/
```


### 3.5 第三阶段：访问验证

#### 3.5.1 端口转发配置

由于虚拟机使用 NAT 模式，需要配置端口转发让组员访问：

1. 关闭虚拟机：`sudo shutdown now`
2. VirtualBox → 设置 → 网络 → 高级 → 端口转发
3. 添加规则：
   - **名称**：`web`
   - **协议**：`TCP`
   - **主机端口**：`8080`
   - **子系统端口**：`80`
4. 启动虚拟机

#### 3.5.2 查看宿主机 IP

在 Windows CMD 中：

```bash
ipconfig
```

找到 WLAN 或以太网的 IPv4 地址（如 `10.150.178.86`）。

#### 3.5.3 访问验证

组员在浏览器访问：

```
http://10.150.178.86:8080
```

应看到 Hexo 博客首页。


### 3.6 第四阶段：自动化更新部署

#### 3.6.1 日常更新流程

**组员推送新文章：**

```bash
git add .
git commit -m "添加新文章"
git push origin main
```

**组长在虚拟机中更新部署：**

```bash
cd ~/GroupWork26.7.6
git pull
npm install          # 如有新依赖
hexo clean
hexo generate
sudo cp -r public/* /var/www/html/
```


#### 3.6.2 自动化部署脚本（可选优化）

创建部署脚本 `/home/groupwork/deploy.sh`：

```bash
#!/bin/bash
cd ~/GroupWork26.7.6
git pull
npm install
hexo clean
hexo generate
sudo cp -r public/* /var/www/html/
echo "部署完成！"
```

赋予执行权限：

```bash
chmod +x ~/deploy.sh
```

每次更新只需执行：

```bash
./deploy.sh
```

### 3.7 第五阶段：CI/CD 自动化（GitHub Actions）

#### 3.7.1 创建工作流文件

在项目根目录创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to Server

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build static site
        run: npx hexo generate

      - name: Deploy via SSH
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          source: "public/"
          target: "/var/www/html/"
          strip_components: 1
```

#### 3.7.2 配置 GitHub Secrets

在 GitHub 仓库 → **Settings** → **Secrets** → **Actions** 中添加：

| Secret 名称 | 值 |
|-------------|-----|
| `SERVER_HOST` | 虚拟机 IP 地址 |
| `SERVER_USER` | `groupwork` |
| `SERVER_PASSWORD` | `123456` |

#### 3.7.3 触发自动化部署

当组员向 `main` 分支推送代码时，GitHub Actions 自动执行构建并部署到服务器。



## 4、实验结果

通过本次实验，成功完成了以下目标：

| 检查项 | 状态 |
|--------|------|
| GitHub 私有仓库创建及组员邀请 | ✅ |
| 组员分支开发与 PR 合并流程 | ✅ |
| Nginx 安装与站点配置 | ✅ |
| Hexo 静态站点生成与部署 | ✅ |
| 端口转发配置（宿主机 8080 → 虚拟机 80） | ✅ |
| 组员通过 `宿主机IP:8080` 访问站点 | ✅ |
| 自动化更新部署流程（git pull → hexo g → cp） | ✅ |
| CI/CD 工作流配置（GitHub Actions） | ✅ |
| 全流程串联（开发 → 存储 → 构建 → 部署） | ✅ |

## 5、遇到的问题与解决方案

| 问题现象 | 可能原因 | 解决方案 |
|----------|----------|----------|
| 宿主机无法访问 `10.0.2.15:80` | NAT 模式下端口未映射 | 配置端口转发（宿主机 8080 → 虚拟机 80） |
| 组员访问 `宿主机IP:8080` 超时 | Windows 防火墙拦截 | 暂时关闭防火墙测试，或添加入站规则放行 8080 端口 |
| 端口转发后仍无法访问 | 宿主机 IP 地址变化 | 重新 `ipconfig` 查新 IP，更新发给组员 |
| `sudo cp -r public/* /var/www/html/` 权限被拒 | `/var/www/html/` 属 root 所有 | 执行 `sudo chown -R groupwork:groupwork /var/www/html/` 更改权限 |
| Nginx 显示默认页面而非博客 | 站点配置未启用或优先级问题 | 检查 `/etc/nginx/sites-enabled/` 中是否有博客站点配置 |
| 虚拟机重启后 IP 变化 | NAT 模式 DHCP 分配 | 重新 `ip addr` 查 IP，通知组员更新 |
| GitHub Actions 部署失败 | Secrets 未配置或网络不通 | 检查 Secrets 中的服务器 IP、用户名、密码是否正确 |
| `hexo generate` 报 ESM 错误 | Node.js 版本与 Hexo 不兼容 | 升级 Node.js 到 20.x，更新 Hexo：`npm install hexo@latest` |
| 页面样式丢失 | NexT 主题 CDN 资源加载失败 | 在主题配置中设置 `vendors: local` |

## 6、实验总结

本次实验是《软件开发工具实践》课程的收尾实验，综合运用了前七个实验所学全部技能，完整实现了从代码编写到网站上线的全流程。

### 全流程回顾

| 阶段 | 涉及实验 | 核心操作 |
|------|----------|----------|
| 环境准备 | 实验一、二 | 安装 Node.js、Git、VS Code、Hexo |
| 内容编写 | 实验二、四 | Markdown 写文章，Hexo 本地预览 |
| 版本管理 | 实验二、四 | Git add/commit/push，GitHub 协作 |
| 服务器搭建 | 实验三、五、六 | VirtualBox + Ubuntu Server，用户与权限管理 |
| 远程管理 | 实验七 | SSH 免密登录，SCP/rsync 文件传输 |
| 部署上线 | 实验八 | Nginx 配置，hexo generate，静态文件部署 |

### 关键收获

1. **工具链的完整理解**：从本地开发（VS Code + Hexo）到版本管理（Git + GitHub）到服务器部署（Ubuntu + Nginx），每个环节都是完整链路中不可或缺的一环。

2. **自动化是效率的核心**：通过 CI/CD（GitHub Actions）将构建和部署流程自动化，减少了人工操作的出错风险，提高了交付速度。

3. **团队协作的规范化**：通过分支管理、Pull Request 审核、自动化部署，实现了“组员写文章 → 组长审核 → 自动上线”的规范化流程。

4. **问题排查能力**：本课程中遇到的网络问题、DNS 问题、端口冲突、版本兼容、权限配置等问题，都是实际运维中的常见挑战，解决这些问题的经验非常宝贵。

<!-- endtab -->

{% endtabs %}